[build-system]
# Requirements for building the compiled package
requires = [
        "setuptools >=64",
        "wheel",
        "cython >=3.0.10",
        "numpy >=2.0"
]
build-backend = "setuptools.build_meta"
